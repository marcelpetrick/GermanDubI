"""Developer command line.

The browser is the product's interface; this is a debugging and automation surface. It
calls the same application services the API does, so it is never a second implementation of
the workflow.
"""
