dist
node_modules
src/api/generated
pnpm-lock.yaml

# The e2e suite is linted from here (see the Makefile), so its generated output must be
# ignored from here too. Playwright writes these on every run.
../e2e/test-results
../e2e/playwright-report
../e2e/node_modules
